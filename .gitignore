# ── Python ──────────────────────────────────────────────────────────────────
.env
.venv/
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store

# ── Credentials (NEVER commit service account keys or secrets) ───────────────
*.json
!package.json
!package-lock.json
.streamlit/secrets.toml

# ── Streamlit config (safe to commit, secrets are NOT) ──────────────────────
# .streamlit/config.toml  ← intentionally NOT ignored so it gets committed

# ── Local cache ─────────────────────────────────────────────────────────────
.cache/
